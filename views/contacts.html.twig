<!DOCTYPE html>
<html>
  <head>
    <title>Address Book</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="css/contact-styles.css">
    <link rel="stylesheet" href="/styles.css">
    <script src="js/jquery-3.1.1.js"></script>
    <script src="js/scripts.js"></script>
  </head>
  <body>
    <div class="container">
      <div class="jumbotron green">
        <h1>Address Book</h1>
        <p>Welcome to your new address book.</p>
        <p>Easily store and retrieve you addresses and stay on the up and up.</p>
      </div>
      <!-- jumbotron -->
      <div class="row">
        <div class="col-sm-6">
          <div class="jumbotron blue">
            <h4>Create New Contact</h4>
            <form action="/create-contact" method="post">
              <div class="form-group">
                <label for="name_input">Name: </label>
                <input class="form-control" type="text" name="name_input">
                <label for="phone_number_input">Phone Number: </label>
                <input class="form-control" type="text" name="phone_number_input">
                <label for="address_input">Address: </label>
                <input class="form-control" type="text" name="address_input">
              </div>
              <!-- formgroup -->
              <button class="btn btn-primary" type="submit" name="new_contact_submit">create new contact</button>
            </form>
            <form class="" action="/search_input" method="post">
              <div class="form-group">
                <label for="search_input"></label>
                <input class="form-control" type="text" name="search_input" placeholder="first letter search...">
              </div>
              <button type="submit" name="view_contacts" class="btn btn-info" value="view">search contacts</button>
            </form>
            <form class="" action="/" method="post">
              <button type="submit" name="view_contacts" class="btn btn-warning" value="view">view contacts</button>
            </form>
            <form class="" action="/delete-contacts" method="post">
                <button type="submit" class="btn btn-danger" name="delete_contacts">delete contacts</button>
            </form>
          </div>
          <!-- jumbotron blue -->
        </div>
        <!-- right column form -->
        <div class="col-sm-6">
          <div class="jumbotron purple">
            {% if list_of_contacts is empty %}
                <p>You have no stored addresses. Click on "create new contact" to populate your address book.</p>
            {% else %}
                <p>Here are all your current contacts:</p>
                {% for contact_instance in list_of_contacts %}
                  <ul>
                    <p>{{ contact_instance.getName }}</p>
                    <li>phone number: {{ contact_instance.getPhoneNumber }}</li>
                    <li>address: {{ contact_instance.getAddress }}</li>
                  </ul>
                    {% endfor %}
            {% endif %}
          </div>
          <!-- jumbotron purple -->
        </div>
        <!-- column -->
      </div>
      <!-- row -->
      <nav id="bottom-nav" class="navbar navbar-inverse navbar-static-top" role="navigation">
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <li>
                <a href="https://github.com/elmunoz42"><i class="fa fa-git" aria-hidden="true"></i>Carlos Munoz Kampff</a>
              </li>
            </ul>
          </div>
        </div>  <!-- /.navbar-collapse -->
      </nav>
      <!-- nav-bar -->
    </div>
    <!-- container -->
  </body>
</html>
